import React from 'react'
import classes from './Spinner.css'
/**
* @author
* @function spinner;
**/

const spinner = (props) => {
  return(
    <div className={classes.Hourglass}> Loading... </div>
   )

 }

export default spinner;